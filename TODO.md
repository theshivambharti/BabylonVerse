# Active TODOs

- [ ] Add touch support and responsive UI scaling for mobile devices in the Material Showcase.
- [ ] Implement the `LightingShowcase.ts` framework.
- [ ] Implement the `CameraShowcase.ts` framework.
- [ ] Add visual loaders / loading screens between showcase transitions.
- [ ] Resolve any remaining overlapping UI elements on very small screens (`< 480px`).
